# Crosshair Sync Test 1

Page loaded with 3 subcharts: VOL, RSI(14), KDJ(9,3,3). All visible and rendering data correctly.
Badge shows "Ind 3". 4 TradingView logos = 4 chart instances.
Need to test crosshair sync by hovering over the main chart and checking if vertical line appears in subcharts.
Also need to test the max 4 limit by trying to enable a 5th subchart.
